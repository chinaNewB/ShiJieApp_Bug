<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--toolbar-->
    <style name="ActionBarThemeOverlay">
        <item name="android:background">@color/transparent</item>
        <item name="android:textColorPrimary">@color/transparent</item>
        <item name="colorControlNormal">@color/transparent</item>
        <item name="colorControlHighlight">@color/transparent</item>
    </style>

    <!--自定义dialog-->
    <style name="CustomDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@color/transparent</item>
    </style>

    <!--登录部分公共样式-->
    <style name="ViewBase">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
    </style>


    <!--登录页输入框样式-->
    <style name="LoginEditText" parent="EditTextBase">
        <item name="android:layout_width">300dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">13dp</item>
        <item name="android:paddingBottom">13dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">20sp</item>
    </style>

    <!--登录页输入框左边图标样式-->
    <style name="LoginImageView">
        <item name="android:layout_width">50dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_centerVertical">true</item>
    </style>

    <!--登录页输入框右边清空图标样式-->
    <style name="LoginImageButton">
        <item name="android:layout_width">30dp</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:visibility">gone</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:src">@mipmap/input_clear</item>
    </style>

    <!--EditText-->
    <style name="EditTextBase">
        <item name="android:textColorHint">@color/color_etv_bg</item>
        <item name="android:textColor">@color/colorBlack</item>
        <item name="android:singleLine">true</item>
        <item name="android:background">@drawable/shape_login_registe_etv</item>
    </style>

    <style name="RegisteEditText" parent="EditTextBase">
        <item name="android:layout_width">280dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">34dp</item>
        <item name="android:padding">13dp</item>
        <item name="android:maxLength">18</item>
    </style>


</resources>
